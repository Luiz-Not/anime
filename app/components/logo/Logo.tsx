export default function Logo({ classNames }: { classNames: string }) {
  return (
    <div className={classNames}>
      <span className="text-white">BUSC</span><span className="text-yellow">ANIME</span>
    </div>
  )
}
